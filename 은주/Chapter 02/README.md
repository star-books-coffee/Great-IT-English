## 1. 초심자의 마음으로, HTML / CSS / JavaScript

- **HTML Intro**
    - `building block` : 구성 요소
    - `besides` : ~외에, 게다가
        - Apart from, Other than, Aside from
        - Other technologies besides HTML are …
    - `aspect` : 속성
        - Links are a fundamental aspect of the Web
    - `annotate` : 주석을 달다
    - `set off` : 구분하다 (set apart), 분리하다
- **CSS Intro**
    - `dialect` : 파생된 언어, 특수 언어
        - XML (including XML dialects such as SVG, MathML or XHTML)
    - `synchronously` : 동기적으로 (순차적으로)
    - `specification` : 사양, 명세
        - The scope of the specification increased significantly
    - `periodically` : 주기적으로
        - This code block fetches the user data from the server periodically.
- **JavaScript Intro**
    - `operator` : 연산자
    - `supplement` : 보충하다
        - JavaScript can be extended for a variety of purposes by supplementing it with additional objects.
    - `Document Object Model (DOM)` : 문서 객체 모델 (웹 페이지를 구조화된 방식으로 표현하여 프로그램이나 스크립트, 웹페이지와 상호작용 할 수 있도록 도움)
    - `invocation` : 실행
- 테스트
    ```
        1. B
        2. D → C 
            - 영문에서 세미콜론은 **앞뒤 문장 간의 연관성이 있음을 내포** (쉼표와 마침표의 중간 성격)
        3. B
        4. C
        5. B 
            - crucial, vital : 중대한, 결정적인
        6. C → D
            - `no matter what` : 막론하고, ~와 무관하게
            - regardless 는 `of` 와 함께 쓰여야 함
        7. C
            - diversion : 전환, 우회
        8. B 
            - meticulous : 꼼꼼한, 세심한
            - tricky, complecx, intricate, difficult : 다루기 힘든, 까다로운
        9. B → A
        10. A
        11. B
            - be made up of / consist of / be composed of / comprise : ~로 구성되다
        12. A → C 
            - By yourself : 혼자서
            - Yourself : 직접, 스스로
        13. B
        14. C
            - denote / represent / indicate / stand for : 의미하다, 나타내다
            - emerge : 나오다, 떠오르다
        15. C
    ``` 
## 2. 간편하고 직관적인 프로그래밍, 파이썬 (Python)

- **파이썬 Intro**
    - `a number of` : 다수의 (more than two, but fewer than many)
    - `indentation` : 들여쓰기
- 테스트
  ```
    1. D
    2. B → D
        - 인터넷, SNS, 어플리케이션, 플랫폼 등 온라인 서비스 상에서 발생하는 상황은 대부분 `on` 전치 사용 (On FaceBook, Instagram … )
    3. C
        - Distinguished, Differentiated, Determined, Discerned : 구분된, 구별된
    4. D
    5. C
    6. A
    7. A → B
        - besides : ~외에 (= aside from / apart from)
    8. C
    9. D → B
        - concatenated, glued together : 연결된, 이어진
        - detached : 떨어져 있는, 분리된
    10. B
        - compound : 복합의, 합성의
        - sole : 유일한, 단독의
    11. D
    12. A → B
        - happen to : 우연히 ~하게 되다, 어쩌다 ~ 하게 되다
        - intentionally, deliberately, on purpose : 고의로, 의도적으로
    13. C
    14. B
        - uneven : 고르지 않은, 불규칙한
        - coordinated : 조직화된
    15. D

## 3. 프론트엔드의 절대 강자, 리액트 (React)

- **React Intro**
    - `declarative` : 선언적인
        - 선언형 프로그래밍 : 프로그램이 “어떻게” 동작하는지가 아닌 “무엇” 을 하는지에 초점을 두는 스타일
    - `Keep A out of B` : A 를 B 로부터 분리하다, 방지하다
        - keep state out of the DOM : 상태와 웹페이지의 문서 객체 모델(DOM)을 분리한다
    - Arbitrary : 임의의
        - 주로 임시적으로, 임의적으로 수행하는 작업이나 변경사항을 나타날 때 사용
